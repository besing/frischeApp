'use strict';

const appConfig = {
  apiCredentials_test: { // switch to Production config later
    url: 'INSERT SHOPWARE GENERAL API ENDPOINT http://.../api',
    apiUser: 'INSERT API USER',
    apiKey: 'INSERT API KEY'
  }
};

export default appConfig;
